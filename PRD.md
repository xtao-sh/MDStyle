# MD Style 产品需求文档

版本：v0.2.0

日期：2026-07-18

状态：当前实现基线

## 1. 产品定位

MD Style 是一款本地优先的 Markdown 富文本排版工具。用户在左侧编辑 Markdown，在右侧实时预览，选择文档模板、视觉主题和发布目标后，可一键复制带内联样式的富文本，粘贴到微信公众号、邮件或其他富文本编辑器。

产品同时承担三个任务：

- 将 Markdown 稳定转换为可粘贴的富文本。
- 为不同内容场景提供结构模板与视觉样式。
- 在不要求账号和云服务的前提下管理多篇本地文档。

## 2. 目标用户与场景

目标用户：

- 使用 Markdown 写作的内容创作者。
- 公众号、Newsletter 和邮件运营人员。
- 产品、商业、教学与研究文档作者。
- 从 AI 工具、Obsidian、Typora 或代码仓库取得 Markdown 的用户。

主要场景：

1. 粘贴或导入 Markdown，实时检查结构与排版。
2. 从场景模板创建文章、报告、课程、研究笔记或活动内容。
3. 切换视觉主题，并统一调整标题、字体、颜色和内容组件。
4. 复制富文本，粘贴到目标平台继续发布。
5. 用目录、标签和搜索管理多篇本地文档。
6. 导出文档库、恢复导入前快照或桌面历史备份。

## 3. 产品目标

### 3.1 当前目标

- 核心路径保持在“编辑或导入、选择样式、复制”三个阶段内。
- 标题、段落、强调、链接、图片、列表、引用、代码、表格和分割线可稳定渲染。
- 富文本复制尽量保留预览中的字体、颜色、背景、间距、边框、阴影和装饰元素。
- 窄屏换行后不产生固定行高、固定宽度或段落重叠。
- 文档和自定义配置默认只保存在用户本机。
- 导入、迁移、异常退出和双存储冲突不应静默覆盖较新的内容。

### 3.2 非目标

- 当前版本不直接调用微信公众号草稿或发布 API。
- 当前版本不提供账号、云同步、多人协作或审核流。
- 当前版本不承诺所有第三方编辑器中像素级一致。
- 当前版本不上传本地图片到远程平台。
- 当前版本不执行用户输入的 HTML、JavaScript 或任意 CSS。

## 4. 信息架构

首屏直接进入工作台：

- 顶栏：文档标题、保存状态、新建、导入、样式、检查和复制。
- 左侧文档库：目录、标签、搜索、排序和文档列表。
- 中间编辑器：可编辑 Markdown 原文。
- 中间预览：按移动或桌面宽度实时渲染。
- 右侧面板：样式、模板、兼容性和导出。
- 底部状态栏：保存状态、字符数、HTML 体积、图片和链接数量。

在宽度不超过 1320px 时，左右侧栏变为抽屉。抽屉需要遮罩、焦点进入、Tab 焦点约束、Escape 关闭和焦点返回。

## 5. 功能需求

### 5.1 Markdown 编辑与渲染

- 左侧输入变化后立即更新右侧预览。
- 支持 `.md` 与 `.markdown` 文件导入，单文件上限 5 MB。
- 支持软换行开关、编辑器全屏和预览全屏。
- 默认使用本地打包的 `markdown-it`，加载失败时使用基础降级解析器。
- 原始 HTML 默认转义，不执行脚本或事件属性。

增强强调识别：

- 将代码之外的全角 `＊` 和 `＿` 规范化为 Markdown 标记。
- 识别强调标记内侧误加空格的情况。
- 识别中文词内双下划线强调。
- 不改写转义标记、行内代码、反引号代码块和波浪线代码块。

链接与图片：

- 链接允许 `http`、`https`、`mailto`、`tel`、锚点和相对路径。
- 拒绝 `javascript:` 与本地 `file:` 链接。
- 图片允许 HTTPS、受限的 Base64 位图和相对路径预览。
- 相对或本地图片、Base64 图片和目标平台不稳定的远程图片必须提示风险。

### 5.2 文档库

- 新建、重命名、删除和切换文档。
- 新建、重命名和删除目录；删除目录时文档迁移到“未分类”。
- 新建、重命名、改色和删除标签；删除标签时清理文档、筛选和个人模板引用。
- 文档可移动目录并分配多个标签。
- 可按目录和多个标签筛选，按标题、正文和标签搜索。
- 可按最近编辑、创建时间、字数和标题排序。
- 在筛选条件下新建文档时继承当前目录与标签，并清除会隐藏新文档的搜索词。
- 当前文档不在搜索结果中时保留编辑上下文并显示说明。

每篇文档保存：

- 字符串 ID、标题、手动标题标记和 Markdown。
- 目录、标签、样式、发布目标和文档级样式覆盖。
- 模板来源、创建时间和更新时间。

### 5.3 文档模板

- 提供 22 套内置模板，覆盖通用、内容发布、产品运营、商业协作、教育研究、个人成长和活动创意。
- 支持按场景筛选和关键词搜索。
- 模板包含 Markdown 骨架、推荐主题、标签和发布目标。
- 用户可将当前文档保存为个人模板。
- 个人模板可修改元数据，也可用当前文档刷新内容、目录、标签、样式和目标。
- 个人模板随文档库导出、恢复和迁移。

### 5.4 视觉样式

- 提供 30 套无重复内置主题。
- 主题归入 7 个视觉家族：清爽通用、长文阅读、商务专业、知识教程、媒体叙事、品牌活动和先锋实验。
- 支持“稳健、鲜明、先锋”三档视觉强度筛选。
- 样式卡提供缩略图、适用场景、收藏和悬浮或聚焦大预览。
- 支持键盘聚焦、Enter 或 Space 应用样式。

个人样式生成器支持：

- 名称、用途、视觉强度和标题结构。
- 7 组字体预设、正文字号与行距。
- 文字、主色、强调色、柔和背景、页面和代码配色。
- 结构化变量白名单，不接受任意脚本或 CSS 属性。

文档级“整体调整”分为：

- 字体与标题：标题结构、正文字体、文字色和强调色。
- 页面与段落：页面背景、段落背景和段落间距。
- 内容组件：加粗背景、引用背景、代码背景、表头背景和链接颜色。

### 5.5 发布目标与兼容性

当前提供三种发布目标：

- 通用富文本。
- 微信公众号。
- 邮件 / Newsletter。

检查项：

- 危险 HTML 和不稳定标签。
- 远程、Base64、本地或相对路径图片。
- 表格列数与窄屏风险。
- 围栏代码块长度。
- 标题长度和导出 HTML 体积。

检查必须基于最终内联导出 HTML 统计，而不是只统计预览 DOM。

### 5.6 富文本复制

复制同时提供 `text/html` 和 `text/plain`：

1. 桌面版优先使用受限 Electron 剪贴板桥接。
2. 浏览器优先使用 Clipboard API。
3. 权限或能力不足时使用隐藏可编辑区域降级复制。

导出器要求：

- 只保留安全标签，移除脚本、表单、媒体和嵌入对象。
- 将 computed style 转成内联 CSS。
- 保留字体、字号、字重、颜色、背景、边框、圆角、阴影、间距和对齐。
- 将 `::before` 与 `::after` 装饰物实体化。
- 不复制正文元素的固定宽度和固定高度。
- 将像素行高转换为受限的无单位行高，避免窄屏换行后文字重叠。
- 将列表转换为带显式标记和悬挂缩进的段落结构，多级列表继续保留层级缩进。
- 居中的装饰线使用响应式 `auto` 边距，不保留当前窗口计算出的固定像素边距。
- 图片使用 `max-width:100%` 与自动高度。
- 表格使用 100% 最大宽度和可换行单元格。

### 5.7 导入、导出与恢复

- 导出当前 Markdown、完整 HTML 和完整文档库 JSON。
- 导入文档库前校验结构和 25 MB 上限。
- 替换文档库前创建浏览器安全快照；桌面版同时创建文件快照。
- 支持恢复最近一次导入前快照。
- 桌面版可浏览并恢复最多 10 份安全快照和 3 份轮换备份。
- 恢复历史版本前先为当前库创建新的安全快照。

## 6. 存储与数据一致性

### 6.1 浏览器

- 使用 `localStorage`，键名保持向后兼容。
- 保存记录使用统一信封：

```json
{
  "version": 2,
  "savedAt": "2026-07-18T08:00:00.000Z",
  "revision": 42,
  "state": {}
}
```

- 编辑后立即显示“保存中”，450ms 防抖后保存。
- 关闭或隐藏页面时同步刷新浏览器副本。
- 存储失败时显示错误或降级状态，不伪装成已保存。

### 6.2 Electron 桌面版

- 同一份信封写入用户数据目录下的 JSON 文件。
- 写入使用临时文件与原子重命名。
- 写入队列保持调用顺序。
- 轮换备份间隔不低于 5 分钟，保留 3 份。
- 浏览器与文件副本冲突时先比较 `savedAt`，相同时间再比较 `revision`，载入较新版本并同步另一侧。
- 解析失败或结构无效的副本不得覆盖有效文档库。

### 6.3 迁移与清洗

- 所有 ID 统一为字符串，兼容旧 JSON 中的数字 ID。
- 无效目录、标签、样式和发布目标回退到安全默认值。
- 重复 ID 自动修复。
- CSS 颜色、字体变量、字号、行距和样式覆盖使用白名单与范围限制。
- 已移除的旧主题 ID 映射到替代主题。

## 7. 技术架构

当前实现不使用 React 或 Vite。应用由静态 HTML、CSS 和浏览器 JavaScript 模块组成，桌面外壳使用 Electron。

主要模块：

```text
index.html                         工作台结构与资源装配
assets/app.js                     状态协调、视图与工作流
assets/library-model.js           默认数据、迁移与清洗
assets/markdown-engine.js         Markdown 增强与安全 URL
assets/wechat-exporter.js         平台中立的富文本内联导出器
assets/theme-catalog.js           主题元数据和缩略图
assets/themes.css                 主题与文档级覆盖
assets/document-templates.js      文档模板目录
assets/publishing-profiles.js     发布目标与阈值
electron/main.js                  窗口、安全策略和 IPC
electron/preload.js               受限剪贴板与存储桥接
electron/library-storage.js       原子存储、备份和恢复点
```

安全边界：

- Electron 开启 context isolation 与 sandbox，关闭 Node integration。
- preload 只暴露剪贴板和文档库存储方法。
- IPC 校验请求来源必须是应用目录内的 `file:` 页面。
- 外部导航只允许 HTTP 与 HTTPS，并交给系统浏览器。
- CSP 禁止外部脚本、对象和表单提交。

## 8. 可用性与无障碍

- 所有图标按钮提供标题或可访问名称。
- 动态目录、文档和样式卡支持键盘操作并暴露选中状态。
- 标签使用原生按钮并暴露 pressed 状态。
- 右侧标签页使用 tablist、tab 与 tabpanel 语义。
- 确认对话框支持 Escape 取消。
- Toast 使用状态播报区域。
- 窄屏抽屉打开时主工作区设为 inert，焦点不离开抽屉。
- 固定格式控件使用稳定尺寸，避免动态内容导致布局跳动。

## 9. 验收标准

- 输入包含标题、段落、强调、链接、图片、三层列表、引用、代码和表格的 Markdown，预览结构正确。
- 复制 HTML 中不含脚本、事件属性、正文固定宽高或异常像素行高。
- 将复制内容放入较窄容器时，文字正常换行且不与下一段重叠。
- 三层列表的标记、悬挂缩进和层级缩进仍可辨识。
- 文档级字体、段落背景、引用、代码、表头与链接覆盖同时出现在预览和复制结果中。
- 目录和标签筛选后新建文档仍可见。
- 重命名或删除标签后不留下失效引用。
- 浏览器旧副本与桌面新副本冲突时选择新副本；同时间戳用 revision 决胜。
- 导入损坏、超大或结构无效的备份时不覆盖现有文档库。
- 760px 以上桌面窗口及常见手机宽度均无不可恢复的遮挡或溢出。

## 10. 测试与发布

自动化命令：

```bash
npm test
npm run test:copy
npm run package
```

- `npm test` 覆盖语法、目录数据、Markdown、安全清洗、迁移、存储与发布配置。
- `npm run test:copy` 启动真实 Electron 渲染进程，覆盖模板、主题、组件覆盖、复制、窄屏、恢复历史、保存状态和存储冲突。
- `npm run package` 生成本机架构的 macOS `.app`。
- GitHub tag 构建 macOS DMG/ZIP 与 Windows 安装版/便携版 EXE。

## 11. 已知限制与后续方向

高优先级后续项：

- 增加导入图片资产与目标平台图片上传适配器。
- 增加手动快照、快照命名与恢复差异预览。
- 增加文档归档、收藏和批量移动。
- 增加更多发布目标的专属规则，例如知识库、博客 CMS 和协作文档。
- 将体量持续增长的 `app.js` 拆为存储、文档库、样式、模板、兼容性和 UI 控制器模块。

长期可选项：

- 端到端加密的多设备同步。
- 团队模板与品牌样式包。
- 图片代理和平台 API 草稿发布。
- 数学公式、脚注和目录的目标平台降级策略。

## 12. 隐私与开源

- 默认不上传文档、标签、目录、模板或样式。
- 仓库只包含框架、内置示例和测试数据，不包含用户运行时文档库。
- 开源协议为 MIT。
